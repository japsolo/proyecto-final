<%# La función include() nos permite pasar variables al archivo que estamos incluyendo, en este caso le pasamos una variable "title" para poderla imprimir dentro de la etiqueta <title>, así tenemos un texto en la pestañan del browser. %>
<%- include('partials/head', { title: 'Detalle del producto' }); %>

<body>

	<%- include('partials/navbar'); %>

	<div class="container">
		<div class="row">
			<div class="col-sm-6 col-lg-10 col-sm-offset-3 col-lg-offset-1">
				<div class="thumbnail">
   				<img src=<%= '/public/images/products/' + product.image %> alt="pdto image">
   				<div class="caption">
     					<h3><%= product.name %></h3>
     					<p><b>$ <%= product.price %></b></p>
     					<p><%= product.shortDescription %></p>
     					<p><%= product.longDescription %></p>
						<form action="/product/delete/<%= product._id %>" method="post">
							<button type="submit" class="btn btn-danger">Eliminar</button>
							<a href="/product/edit/<%= product._id %>" class="btn btn-warning">Editar</a>
							<a href="/" class="btn btn-info">volver</a>
						</form>
   				</div>
 				</div>
			</div>
		</div>
	</div>

	<%- include('partials/scripts.ejs') %>
</body>
</html>
